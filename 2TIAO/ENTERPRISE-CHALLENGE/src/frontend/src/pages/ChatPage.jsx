import ChatWindow from '../components/chat/ChatWindow';

/**
 * ChatPage — página do chat (A5). A lógica vive em ChatWindow; aqui fica só o
 * container da rota. Espaço reservado para exibir, no futuro, o resumo/aviso
 * gerado pela Nathalia (N3–N5) quando o formato for definido.
 */
export default function ChatPage() {
  return <ChatWindow />;
}
